@model Bieb.Web.Models.Books.ViewBookModel

@* Placeholder view. We need infrastructure for actual cover pictures before we can finish this view. *@

<div class="cover">
    <div class="back col-xs-4">
        <p class="publisher">
            @if (Model.Publisher != null)
            {
                @Model.Publisher.Text
            }
        </p>
    </div>
    <div class="front col-xs-4">
        <p class="main-person">
            @if (Model.Editors.Any())
            {
                @Html.Raw(string.Join(", ", Model.Editors.Select(person => person.Text)))
            }
            else if (Model.Authors.Any())
            {
                @Html.Raw(string.Join(", ", Model.Authors.Select(person => person.Text)))
            }
        </p>
        <p class="title">
            @Model.Title
        </p>
    </div>
</div>