@model IEnumerable<Bieb.Domain.Entities.Person>

<ul>
@foreach (var person in Model) 
{
    <li>
        <h3>@Html.ActionLink(person.FullName, "Details", new { controller = "Person", id = person.Id })</h3>
    </li>
}
</ul>
